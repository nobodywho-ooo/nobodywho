// This file was autogenerated by some hot garbage in the `uniffi` crate.
// Trust me, you don't want to mess with it!

@file:Suppress("NAME_SHADOWING")

package uniffi.nobodywho

// Common helper code.
//
// Ideally this would live in a separate .kt file where it can be unittested etc
// in isolation, and perhaps even published as a re-useable package.
//
// However, it's important that the details of how this helper code works (e.g. the
// way that different builtin types are passed across the FFI) exactly match what's
// expected by the Rust code on the other side of the interface. In practice right
// now that means coming from the exact some version of `uniffi` that was used to
// compile the Rust component. The easiest way to ensure this is to bundle the Kotlin
// helpers directly inline like we're doing here.

import com.sun.jna.Library
import com.sun.jna.IntegerType
import com.sun.jna.Native
import com.sun.jna.Pointer
import com.sun.jna.Structure
import com.sun.jna.Callback
import com.sun.jna.ptr.*
import java.nio.ByteBuffer
import java.nio.ByteOrder
import java.nio.CharBuffer
import java.nio.charset.CodingErrorAction
import java.util.concurrent.atomic.AtomicLong
import java.util.concurrent.ConcurrentHashMap
import java.util.concurrent.atomic.AtomicBoolean
import kotlin.coroutines.resume
import kotlinx.coroutines.CancellableContinuation
import kotlinx.coroutines.DelicateCoroutinesApi
import kotlinx.coroutines.GlobalScope
import kotlinx.coroutines.Job
import kotlinx.coroutines.launch
import kotlinx.coroutines.suspendCancellableCoroutine

// This is a helper for safely working with byte buffers returned from the Rust code.
// A rust-owned buffer is represented by its capacity, its current length, and a
// pointer to the underlying data.

/**
 * @suppress
 */
@Structure.FieldOrder("capacity", "len", "data")
open class RustBuffer : Structure() {
    // Note: `capacity` and `len` are actually `ULong` values, but JVM only supports signed values.
    // When dealing with these fields, make sure to call `toULong()`.
    @JvmField var capacity: Long = 0
    @JvmField var len: Long = 0
    @JvmField var data: Pointer? = null

    class ByValue: RustBuffer(), Structure.ByValue
    class ByReference: RustBuffer(), Structure.ByReference

   internal fun setValue(other: RustBuffer) {
        capacity = other.capacity
        len = other.len
        data = other.data
    }

    companion object {
        internal fun alloc(size: ULong = 0UL) = uniffiRustCall() { status ->
            // Note: need to convert the size to a `Long` value to make this work with JVM.
            UniffiLib.ffi_nobodywho_uniffi_rustbuffer_alloc(size.toLong(), status)
        }.also {
            if(it.data == null) {
               throw RuntimeException("RustBuffer.alloc() returned null data pointer (size=${size})")
           }
        }

        internal fun create(capacity: ULong, len: ULong, data: Pointer?): RustBuffer.ByValue {
            var buf = RustBuffer.ByValue()
            buf.capacity = capacity.toLong()
            buf.len = len.toLong()
            buf.data = data
            return buf
        }

        internal fun free(buf: RustBuffer.ByValue) = uniffiRustCall() { status ->
            UniffiLib.ffi_nobodywho_uniffi_rustbuffer_free(buf, status)
        }
    }

    @Suppress("TooGenericExceptionThrown")
    fun asByteBuffer() =
        this.data?.getByteBuffer(0, this.len.toLong())?.also {
            it.order(ByteOrder.BIG_ENDIAN)
        }
}

// This is a helper for safely passing byte references into the rust code.
// It's not actually used at the moment, because there aren't many things that you
// can take a direct pointer to in the JVM, and if we're going to copy something
// then we might as well copy it into a `RustBuffer`. But it's here for API
// completeness.

@Structure.FieldOrder("len", "data")
internal open class ForeignBytes : Structure() {
    @JvmField var len: Int = 0
    @JvmField var data: Pointer? = null

    class ByValue : ForeignBytes(), Structure.ByValue
}
/**
 * The FfiConverter interface handles converter types to and from the FFI
 *
 * All implementing objects should be public to support external types.  When a
 * type is external we need to import it's FfiConverter.
 *
 * @suppress
 */
public interface FfiConverter<KotlinType, FfiType> {
    // Convert an FFI type to a Kotlin type
    fun lift(value: FfiType): KotlinType

    // Convert an Kotlin type to an FFI type
    fun lower(value: KotlinType): FfiType

    // Read a Kotlin type from a `ByteBuffer`
    fun read(buf: ByteBuffer): KotlinType

    // Calculate bytes to allocate when creating a `RustBuffer`
    //
    // This must return at least as many bytes as the write() function will
    // write. It can return more bytes than needed, for example when writing
    // Strings we can't know the exact bytes needed until we the UTF-8
    // encoding, so we pessimistically allocate the largest size possible (3
    // bytes per codepoint).  Allocating extra bytes is not really a big deal
    // because the `RustBuffer` is short-lived.
    fun allocationSize(value: KotlinType): ULong

    // Write a Kotlin type to a `ByteBuffer`
    fun write(value: KotlinType, buf: ByteBuffer)

    // Lower a value into a `RustBuffer`
    //
    // This method lowers a value into a `RustBuffer` rather than the normal
    // FfiType.  It's used by the callback interface code.  Callback interface
    // returns are always serialized into a `RustBuffer` regardless of their
    // normal FFI type.
    fun lowerIntoRustBuffer(value: KotlinType): RustBuffer.ByValue {
        val rbuf = RustBuffer.alloc(allocationSize(value))
        try {
            val bbuf = rbuf.data!!.getByteBuffer(0, rbuf.capacity).also {
                it.order(ByteOrder.BIG_ENDIAN)
            }
            write(value, bbuf)
            rbuf.writeField("len", bbuf.position().toLong())
            return rbuf
        } catch (e: Throwable) {
            RustBuffer.free(rbuf)
            throw e
        }
    }

    // Lift a value from a `RustBuffer`.
    //
    // This here mostly because of the symmetry with `lowerIntoRustBuffer()`.
    // It's currently only used by the `FfiConverterRustBuffer` class below.
    fun liftFromRustBuffer(rbuf: RustBuffer.ByValue): KotlinType {
        val byteBuf = rbuf.asByteBuffer()!!
        try {
           val item = read(byteBuf)
           if (byteBuf.hasRemaining()) {
               throw RuntimeException("junk remaining in buffer after lifting, something is very wrong!!")
           }
           return item
        } finally {
            RustBuffer.free(rbuf)
        }
    }
}

/**
 * FfiConverter that uses `RustBuffer` as the FfiType
 *
 * @suppress
 */
public interface FfiConverterRustBuffer<KotlinType>: FfiConverter<KotlinType, RustBuffer.ByValue> {
    override fun lift(value: RustBuffer.ByValue) = liftFromRustBuffer(value)
    override fun lower(value: KotlinType) = lowerIntoRustBuffer(value)
}
// A handful of classes and functions to support the generated data structures.
// This would be a good candidate for isolating in its own ffi-support lib.

internal const val UNIFFI_CALL_SUCCESS = 0.toByte()
internal const val UNIFFI_CALL_ERROR = 1.toByte()
internal const val UNIFFI_CALL_UNEXPECTED_ERROR = 2.toByte()

@Structure.FieldOrder("code", "error_buf")
internal open class UniffiRustCallStatus : Structure() {
    @JvmField var code: Byte = 0
    @JvmField var error_buf: RustBuffer.ByValue = RustBuffer.ByValue()

    class ByValue: UniffiRustCallStatus(), Structure.ByValue

    fun isSuccess(): Boolean {
        return code == UNIFFI_CALL_SUCCESS
    }

    fun isError(): Boolean {
        return code == UNIFFI_CALL_ERROR
    }

    fun isPanic(): Boolean {
        return code == UNIFFI_CALL_UNEXPECTED_ERROR
    }

    companion object {
        fun create(code: Byte, errorBuf: RustBuffer.ByValue): UniffiRustCallStatus.ByValue {
            val callStatus = UniffiRustCallStatus.ByValue()
            callStatus.code = code
            callStatus.error_buf = errorBuf
            return callStatus
        }
    }
}

class InternalException(message: String) : kotlin.Exception(message)

/**
 * Each top-level error class has a companion object that can lift the error from the call status's rust buffer
 *
 * @suppress
 */
interface UniffiRustCallStatusErrorHandler<E> {
    fun lift(error_buf: RustBuffer.ByValue): E;
}

// Helpers for calling Rust
// In practice we usually need to be synchronized to call this safely, so it doesn't
// synchronize itself

// Call a rust function that returns a Result<>.  Pass in the Error class companion that corresponds to the Err
private inline fun <U, E: kotlin.Exception> uniffiRustCallWithError(errorHandler: UniffiRustCallStatusErrorHandler<E>, callback: (UniffiRustCallStatus) -> U): U {
    var status = UniffiRustCallStatus()
    val return_value = callback(status)
    uniffiCheckCallStatus(errorHandler, status)
    return return_value
}

// Check UniffiRustCallStatus and throw an error if the call wasn't successful
private fun<E: kotlin.Exception> uniffiCheckCallStatus(errorHandler: UniffiRustCallStatusErrorHandler<E>, status: UniffiRustCallStatus) {
    if (status.isSuccess()) {
        return
    } else if (status.isError()) {
        throw errorHandler.lift(status.error_buf)
    } else if (status.isPanic()) {
        // when the rust code sees a panic, it tries to construct a rustbuffer
        // with the message.  but if that code panics, then it just sends back
        // an empty buffer.
        if (status.error_buf.len > 0) {
            throw InternalException(FfiConverterString.lift(status.error_buf))
        } else {
            throw InternalException("Rust panic")
        }
    } else {
        throw InternalException("Unknown rust call status: $status.code")
    }
}

/**
 * UniffiRustCallStatusErrorHandler implementation for times when we don't expect a CALL_ERROR
 *
 * @suppress
 */
object UniffiNullRustCallStatusErrorHandler: UniffiRustCallStatusErrorHandler<InternalException> {
    override fun lift(error_buf: RustBuffer.ByValue): InternalException {
        RustBuffer.free(error_buf)
        return InternalException("Unexpected CALL_ERROR")
    }
}

// Call a rust function that returns a plain value
private inline fun <U> uniffiRustCall(callback: (UniffiRustCallStatus) -> U): U {
    return uniffiRustCallWithError(UniffiNullRustCallStatusErrorHandler, callback)
}

internal inline fun<T> uniffiTraitInterfaceCall(
    callStatus: UniffiRustCallStatus,
    makeCall: () -> T,
    writeReturn: (T) -> Unit,
) {
    try {
        writeReturn(makeCall())
    } catch(e: kotlin.Exception) {
        callStatus.code = UNIFFI_CALL_UNEXPECTED_ERROR
        callStatus.error_buf = FfiConverterString.lower(e.toString())
    }
}

internal inline fun<T, reified E: Throwable> uniffiTraitInterfaceCallWithError(
    callStatus: UniffiRustCallStatus,
    makeCall: () -> T,
    writeReturn: (T) -> Unit,
    lowerError: (E) -> RustBuffer.ByValue
) {
    try {
        writeReturn(makeCall())
    } catch(e: kotlin.Exception) {
        if (e is E) {
            callStatus.code = UNIFFI_CALL_ERROR
            callStatus.error_buf = lowerError(e)
        } else {
            callStatus.code = UNIFFI_CALL_UNEXPECTED_ERROR
            callStatus.error_buf = FfiConverterString.lower(e.toString())
        }
    }
}
// Initial value and increment amount for handles. 
// These ensure that Kotlin-generated handles always have the lowest bit set
private const val UNIFFI_HANDLEMAP_INITIAL = 1.toLong()
private const val UNIFFI_HANDLEMAP_DELTA = 2.toLong()

// Map handles to objects
//
// This is used pass an opaque 64-bit handle representing a foreign object to the Rust code.
internal class UniffiHandleMap<T: Any> {
    private val map = ConcurrentHashMap<Long, T>()
    // Start 
    private val counter = java.util.concurrent.atomic.AtomicLong(UNIFFI_HANDLEMAP_INITIAL)

    val size: Int
        get() = map.size

    // Insert a new object into the handle map and get a handle for it
    fun insert(obj: T): Long {
        val handle = counter.getAndAdd(UNIFFI_HANDLEMAP_DELTA)
        map.put(handle, obj)
        return handle
    }

    // Clone a handle, creating a new one
    fun clone(handle: Long): Long {
        val obj = map.get(handle) ?: throw InternalException("UniffiHandleMap.clone: Invalid handle")
        return insert(obj)
    }

    // Get an object from the handle map
    fun get(handle: Long): T {
        return map.get(handle) ?: throw InternalException("UniffiHandleMap.get: Invalid handle")
    }

    // Remove an entry from the handlemap and get the Kotlin object back
    fun remove(handle: Long): T {
        return map.remove(handle) ?: throw InternalException("UniffiHandleMap: Invalid handle")
    }
}

// Contains loading, initialization code,
// and the FFI Function declarations in a com.sun.jna.Library.
@Synchronized
private fun findLibraryName(componentName: String): String {
    val libOverride = System.getProperty("uniffi.component.$componentName.libraryOverride")
    if (libOverride != null) {
        return libOverride
    }
    return "nobodywho_uniffi"
}

// Define FFI callback types
internal interface UniffiRustFutureContinuationCallback : com.sun.jna.Callback {
    fun callback(`data`: Long,`pollResult`: Byte,)
}
internal interface UniffiForeignFutureDroppedCallback : com.sun.jna.Callback {
    fun callback(`handle`: Long,)
}
internal interface UniffiCallbackInterfaceFree : com.sun.jna.Callback {
    fun callback(`handle`: Long,)
}
internal interface UniffiCallbackInterfaceClone : com.sun.jna.Callback {
    fun callback(`handle`: Long,)
    : Long
}
@Structure.FieldOrder("handle", "free")
internal open class UniffiForeignFutureDroppedCallbackStruct(
    @JvmField internal var `handle`: Long = 0.toLong(),
    @JvmField internal var `free`: UniffiForeignFutureDroppedCallback? = null,
) : Structure() {
    class UniffiByValue(
        `handle`: Long = 0.toLong(),
        `free`: UniffiForeignFutureDroppedCallback? = null,
    ): UniffiForeignFutureDroppedCallbackStruct(`handle`,`free`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureDroppedCallbackStruct) {
        `handle` = other.`handle`
        `free` = other.`free`
    }

}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultU8(
    @JvmField internal var `returnValue`: Byte = 0.toByte(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Byte = 0.toByte(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultU8(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultU8) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteU8 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultU8.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultI8(
    @JvmField internal var `returnValue`: Byte = 0.toByte(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Byte = 0.toByte(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultI8(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultI8) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteI8 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultI8.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultU16(
    @JvmField internal var `returnValue`: Short = 0.toShort(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Short = 0.toShort(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultU16(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultU16) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteU16 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultU16.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultI16(
    @JvmField internal var `returnValue`: Short = 0.toShort(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Short = 0.toShort(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultI16(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultI16) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteI16 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultI16.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultU32(
    @JvmField internal var `returnValue`: Int = 0,
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Int = 0,
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultU32(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultU32) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteU32 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultU32.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultI32(
    @JvmField internal var `returnValue`: Int = 0,
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Int = 0,
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultI32(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultI32) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteI32 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultI32.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultU64(
    @JvmField internal var `returnValue`: Long = 0.toLong(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Long = 0.toLong(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultU64(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultU64) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteU64 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultU64.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultI64(
    @JvmField internal var `returnValue`: Long = 0.toLong(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Long = 0.toLong(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultI64(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultI64) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteI64 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultI64.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultF32(
    @JvmField internal var `returnValue`: Float = 0.0f,
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Float = 0.0f,
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultF32(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultF32) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteF32 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultF32.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultF64(
    @JvmField internal var `returnValue`: Double = 0.0,
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Double = 0.0,
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultF64(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultF64) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteF64 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultF64.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultRustBuffer(
    @JvmField internal var `returnValue`: RustBuffer.ByValue = RustBuffer.ByValue(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: RustBuffer.ByValue = RustBuffer.ByValue(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultRustBuffer(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultRustBuffer) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteRustBuffer : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultRustBuffer.UniffiByValue,)
}
@Structure.FieldOrder("callStatus")
internal open class UniffiForeignFutureResultVoid(
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultVoid(`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultVoid) {
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteVoid : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultVoid.UniffiByValue,)
}
internal interface UniffiCallbackInterfaceRustToolCallbackMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`argumentsJson`: RustBuffer.ByValue,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,)
}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "call")
internal open class UniffiVTableCallbackInterfaceRustToolCallback(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `call`: UniffiCallbackInterfaceRustToolCallbackMethod0? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `call`: UniffiCallbackInterfaceRustToolCallbackMethod0? = null,
    ): UniffiVTableCallbackInterfaceRustToolCallback(`uniffiFree`,`uniffiClone`,`call`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceRustToolCallback) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `call` = other.`call`
    }

}

// A JNA Library to expose the extern-C FFI definitions.
// This is an implementation detail which will be called internally by the public API.

// For large crates we prevent `MethodTooLargeException` (see #2340)
// N.B. the name of the extension is very misleading, since it is
// rather `InterfaceTooLargeException`, caused by too many methods
// in the interface for large crates.
//
// By splitting the otherwise huge interface into two parts
// * UniffiLib (this)
// * IntegrityCheckingUniffiLib
// And all checksum methods are put into `IntegrityCheckingUniffiLib`
// we allow for ~2x as many methods in the UniffiLib interface.
//
// Note: above all written when we used JNA's `loadIndirect` etc.
// We now use JNA's "direct mapping" - unclear if same considerations apply exactly.
internal object IntegrityCheckingUniffiLib {
    init {
        Native.register(IntegrityCheckingUniffiLib::class.java, findLibraryName(componentName = "nobodywho"))
        uniffiCheckContractApiVersion(this)
        uniffiCheckApiChecksums(this)
    }
    external fun uniffi_nobodywho_uniffi_checksum_func_cosine_similarity(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_func_load_model(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_func_sampler_preset_default(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_func_sampler_preset_dry(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_func_sampler_preset_grammar(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_func_sampler_preset_greedy(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_func_sampler_preset_json(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_func_sampler_preset_temperature(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_func_sampler_preset_top_k(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_func_sampler_preset_top_p(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_crossencoder_rank(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_crossencoder_rank_and_sort_json(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_encoder_encode(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_ask(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_ask_with_prompt(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_get_chat_history(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_get_sampler_config_json(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_get_system_prompt(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_get_template_variables(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_reset_context(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_reset_history(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_set_chat_history(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_set_sampler_config(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_set_system_prompt(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_set_template_variable(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_set_tools(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rustchat_stop_generation(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rusttokenstream_completed(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rusttokenstream_next_token(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rusttool_get_schema_json(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_dist(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_dry(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_grammar(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_greedy(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_min_p(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_mirostat_v1(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_mirostat_v2(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_penalties(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_temperature(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_top_k(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_top_p(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_typical_p(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_xtc(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_samplerconfig_to_json(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_constructor_crossencoder_new(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_constructor_encoder_new(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_constructor_rustchat_new(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_constructor_rusttool_new(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_constructor_samplerbuilder_new(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_constructor_samplerconfig_from_json(
    ): Short
    external fun uniffi_nobodywho_uniffi_checksum_method_rusttoolcallback_call(
    ): Short
    external fun ffi_nobodywho_uniffi_uniffi_contract_version(
    ): Int
    
        
}

internal object UniffiLib {
    
    // The Cleaner for the whole library
    internal val CLEANER: UniffiCleaner by lazy {
        UniffiCleaner.create()
    }
    

    init {
        Native.register(UniffiLib::class.java, findLibraryName(componentName = "nobodywho"))
        uniffiCallbackInterfaceRustToolCallback.register(this)
        
    }
    external fun uniffi_nobodywho_uniffi_fn_clone_crossencoder(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_free_crossencoder(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Unit
external fun uniffi_nobodywho_uniffi_fn_constructor_crossencoder_new(`model`: Long,`contextSize`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_crossencoder_rank(`ptr`: Long,`query`: RustBuffer.ByValue,`documents`: RustBuffer.ByValue,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_crossencoder_rank_and_sort_json(`ptr`: Long,`query`: RustBuffer.ByValue,`documents`: RustBuffer.ByValue,
): Long
external fun uniffi_nobodywho_uniffi_fn_clone_encoder(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_free_encoder(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Unit
external fun uniffi_nobodywho_uniffi_fn_constructor_encoder_new(`model`: Long,`contextSize`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_encoder_encode(`ptr`: Long,`text`: RustBuffer.ByValue,
): Long
external fun uniffi_nobodywho_uniffi_fn_clone_model(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_free_model(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Unit
external fun uniffi_nobodywho_uniffi_fn_clone_rustchat(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_free_rustchat(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Unit
external fun uniffi_nobodywho_uniffi_fn_constructor_rustchat_new(`model`: Long,`systemPrompt`: RustBuffer.ByValue,`contextSize`: Int,`templateVariables`: RustBuffer.ByValue,`tools`: RustBuffer.ByValue,`sampler`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_ask(`ptr`: Long,`message`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_ask_with_prompt(`ptr`: Long,`parts`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_get_chat_history(`ptr`: Long,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_get_sampler_config_json(`ptr`: Long,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_get_system_prompt(`ptr`: Long,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_get_template_variables(`ptr`: Long,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_reset_context(`ptr`: Long,`systemPrompt`: RustBuffer.ByValue,`tools`: RustBuffer.ByValue,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_reset_history(`ptr`: Long,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_set_chat_history(`ptr`: Long,`messages`: RustBuffer.ByValue,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_set_sampler_config(`ptr`: Long,`sampler`: Long,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_set_system_prompt(`ptr`: Long,`systemPrompt`: RustBuffer.ByValue,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_set_template_variable(`ptr`: Long,`name`: RustBuffer.ByValue,`value`: Byte,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_set_tools(`ptr`: Long,`tools`: RustBuffer.ByValue,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rustchat_stop_generation(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Unit
external fun uniffi_nobodywho_uniffi_fn_clone_rusttokenstream(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_free_rusttokenstream(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Unit
external fun uniffi_nobodywho_uniffi_fn_method_rusttokenstream_completed(`ptr`: Long,
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rusttokenstream_next_token(`ptr`: Long,
): Long
external fun uniffi_nobodywho_uniffi_fn_clone_rusttool(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_free_rusttool(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Unit
external fun uniffi_nobodywho_uniffi_fn_constructor_rusttool_new(`name`: RustBuffer.ByValue,`description`: RustBuffer.ByValue,`parameters`: RustBuffer.ByValue,`callback`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_rusttool_get_schema_json(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
): RustBuffer.ByValue
external fun uniffi_nobodywho_uniffi_fn_clone_samplerbuilder(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_free_samplerbuilder(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Unit
external fun uniffi_nobodywho_uniffi_fn_constructor_samplerbuilder_new(uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_dist(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_dry(`ptr`: Long,`multiplier`: Float,`base`: Float,`allowedLength`: Int,`penaltyLastN`: Int,`seqBreakers`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_grammar(`ptr`: Long,`grammar`: RustBuffer.ByValue,`triggerOn`: RustBuffer.ByValue,`root`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_greedy(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_min_p(`ptr`: Long,`minP`: Float,`minKeep`: Int,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_mirostat_v1(`ptr`: Long,`tau`: Float,`eta`: Float,`m`: Int,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_mirostat_v2(`ptr`: Long,`tau`: Float,`eta`: Float,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_penalties(`ptr`: Long,`penaltyLastN`: Int,`penaltyRepeat`: Float,`penaltyFreq`: Float,`penaltyPresent`: Float,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_temperature(`ptr`: Long,`temperature`: Float,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_top_k(`ptr`: Long,`topK`: Int,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_top_p(`ptr`: Long,`topP`: Float,`minKeep`: Int,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_typical_p(`ptr`: Long,`typP`: Float,`minKeep`: Int,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerbuilder_xtc(`ptr`: Long,`xtcProbability`: Float,`xtcThreshold`: Float,`minKeep`: Int,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_clone_samplerconfig(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_free_samplerconfig(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Unit
external fun uniffi_nobodywho_uniffi_fn_constructor_samplerconfig_from_json(`jsonStr`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_method_samplerconfig_to_json(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
): RustBuffer.ByValue
external fun uniffi_nobodywho_uniffi_fn_init_callback_vtable_rusttoolcallback(`vtable`: UniffiVTableCallbackInterfaceRustToolCallback,
): Unit
external fun uniffi_nobodywho_uniffi_fn_func_cosine_similarity(`a`: RustBuffer.ByValue,`b`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): Float
external fun uniffi_nobodywho_uniffi_fn_func_load_model(`modelPath`: RustBuffer.ByValue,`useGpu`: Byte,`imageModelPath`: RustBuffer.ByValue,
): Long
external fun uniffi_nobodywho_uniffi_fn_func_sampler_preset_default(uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_func_sampler_preset_dry(uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_func_sampler_preset_grammar(`grammar`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_func_sampler_preset_greedy(uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_func_sampler_preset_json(uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_func_sampler_preset_temperature(`temperature`: Float,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_func_sampler_preset_top_k(`topK`: Int,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun uniffi_nobodywho_uniffi_fn_func_sampler_preset_top_p(`topP`: Float,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun ffi_nobodywho_uniffi_rustbuffer_alloc(`size`: Long,uniffi_out_err: UniffiRustCallStatus, 
): RustBuffer.ByValue
external fun ffi_nobodywho_uniffi_rustbuffer_from_bytes(`bytes`: ForeignBytes.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): RustBuffer.ByValue
external fun ffi_nobodywho_uniffi_rustbuffer_free(`buf`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
): Unit
external fun ffi_nobodywho_uniffi_rustbuffer_reserve(`buf`: RustBuffer.ByValue,`additional`: Long,uniffi_out_err: UniffiRustCallStatus, 
): RustBuffer.ByValue
external fun ffi_nobodywho_uniffi_rust_future_poll_u8(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_u8(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_u8(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_u8(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Byte
external fun ffi_nobodywho_uniffi_rust_future_poll_i8(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_i8(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_i8(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_i8(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Byte
external fun ffi_nobodywho_uniffi_rust_future_poll_u16(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_u16(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_u16(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_u16(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Short
external fun ffi_nobodywho_uniffi_rust_future_poll_i16(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_i16(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_i16(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_i16(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Short
external fun ffi_nobodywho_uniffi_rust_future_poll_u32(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_u32(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_u32(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_u32(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Int
external fun ffi_nobodywho_uniffi_rust_future_poll_i32(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_i32(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_i32(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_i32(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Int
external fun ffi_nobodywho_uniffi_rust_future_poll_u64(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_u64(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_u64(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_u64(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun ffi_nobodywho_uniffi_rust_future_poll_i64(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_i64(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_i64(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_i64(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Long
external fun ffi_nobodywho_uniffi_rust_future_poll_f32(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_f32(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_f32(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_f32(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Float
external fun ffi_nobodywho_uniffi_rust_future_poll_f64(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_f64(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_f64(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_f64(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Double
external fun ffi_nobodywho_uniffi_rust_future_poll_rust_buffer(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_rust_buffer(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_rust_buffer(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_rust_buffer(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): RustBuffer.ByValue
external fun ffi_nobodywho_uniffi_rust_future_poll_void(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_cancel_void(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_free_void(`handle`: Long,
): Unit
external fun ffi_nobodywho_uniffi_rust_future_complete_void(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
): Unit

    
}

private fun uniffiCheckContractApiVersion(lib: IntegrityCheckingUniffiLib) {
    // Get the bindings contract version from our ComponentInterface
    val bindings_contract_version = 30
    // Get the scaffolding contract version by calling the into the dylib
    val scaffolding_contract_version = lib.ffi_nobodywho_uniffi_uniffi_contract_version()
    if (bindings_contract_version != scaffolding_contract_version) {
        throw RuntimeException("UniFFI contract version mismatch: try cleaning and rebuilding your project")
    }
}
@Suppress("UNUSED_PARAMETER")
private fun uniffiCheckApiChecksums(lib: IntegrityCheckingUniffiLib) {
    if (lib.uniffi_nobodywho_uniffi_checksum_func_cosine_similarity() != 63439.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_func_load_model() != 19243.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_func_sampler_preset_default() != 10834.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_func_sampler_preset_dry() != 55378.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_func_sampler_preset_grammar() != 29288.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_func_sampler_preset_greedy() != 13219.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_func_sampler_preset_json() != 40006.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_func_sampler_preset_temperature() != 64803.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_func_sampler_preset_top_k() != 44137.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_func_sampler_preset_top_p() != 54893.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_crossencoder_rank() != 14399.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_crossencoder_rank_and_sort_json() != 4173.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_encoder_encode() != 21765.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_ask() != 53575.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_ask_with_prompt() != 65089.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_get_chat_history() != 12722.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_get_sampler_config_json() != 33078.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_get_system_prompt() != 57727.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_get_template_variables() != 19616.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_reset_context() != 47191.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_reset_history() != 12058.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_set_chat_history() != 6058.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_set_sampler_config() != 28012.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_set_system_prompt() != 31690.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_set_template_variable() != 64000.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_set_tools() != 55680.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rustchat_stop_generation() != 24711.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rusttokenstream_completed() != 26060.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rusttokenstream_next_token() != 44770.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rusttool_get_schema_json() != 4679.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_dist() != 23376.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_dry() != 35315.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_grammar() != 24369.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_greedy() != 32898.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_min_p() != 33705.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_mirostat_v1() != 58563.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_mirostat_v2() != 41682.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_penalties() != 40767.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_temperature() != 8456.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_top_k() != 26600.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_top_p() != 54577.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_typical_p() != 28727.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerbuilder_xtc() != 22853.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_samplerconfig_to_json() != 51798.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_constructor_crossencoder_new() != 12491.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_constructor_encoder_new() != 32094.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_constructor_rustchat_new() != 6355.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_constructor_rusttool_new() != 54104.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_constructor_samplerbuilder_new() != 50214.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_constructor_samplerconfig_from_json() != 6867.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_nobodywho_uniffi_checksum_method_rusttoolcallback_call() != 43958.toShort()) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
}

/**
 * @suppress
 */
public fun uniffiEnsureInitialized() {
    IntegrityCheckingUniffiLib
    // UniffiLib() initialized as objects are used, but we still need to explicitly
    // reference it so initialization across crates works as expected.
    UniffiLib
}

// Async support
// Async return type handlers

internal const val UNIFFI_RUST_FUTURE_POLL_READY = 0.toByte()
internal const val UNIFFI_RUST_FUTURE_POLL_WAKE = 1.toByte()

internal val uniffiContinuationHandleMap = UniffiHandleMap<CancellableContinuation<Byte>>()

// FFI type for Rust future continuations
internal object uniffiRustFutureContinuationCallbackImpl: UniffiRustFutureContinuationCallback {
    override fun callback(data: Long, pollResult: Byte) {
        uniffiContinuationHandleMap.remove(data).resume(pollResult)
    }
}

internal suspend fun<T, F, E: kotlin.Exception> uniffiRustCallAsync(
    rustFuture: Long,
    pollFunc: (Long, UniffiRustFutureContinuationCallback, Long) -> Unit,
    completeFunc: (Long, UniffiRustCallStatus) -> F,
    freeFunc: (Long) -> Unit,
    liftFunc: (F) -> T,
    errorHandler: UniffiRustCallStatusErrorHandler<E>
): T {
    try {
        do {
            val pollResult = suspendCancellableCoroutine<Byte> { continuation ->
                pollFunc(
                    rustFuture,
                    uniffiRustFutureContinuationCallbackImpl,
                    uniffiContinuationHandleMap.insert(continuation)
                )
            }
        } while (pollResult != UNIFFI_RUST_FUTURE_POLL_READY);

        return liftFunc(
            uniffiRustCallWithError(errorHandler, { status -> completeFunc(rustFuture, status) })
        )
    } finally {
        freeFunc(rustFuture)
    }
}

// Public interface members begin here.


// Interface implemented by anything that can contain an object reference.
//
// Such types expose a `destroy()` method that must be called to cleanly
// dispose of the contained objects. Failure to call this method may result
// in memory leaks.
//
// The easiest way to ensure this method is called is to use the `.use`
// helper method to execute a block and destroy the object at the end.
interface Disposable {
    fun destroy()
    companion object {
        fun destroy(vararg args: Any?) {
            for (arg in args) {
                when (arg) {
                    is Disposable -> arg.destroy()
                    is ArrayList<*> -> {
                        for (idx in arg.indices) {
                            val element = arg[idx]
                            if (element is Disposable) {
                                element.destroy()
                            }
                        }
                    }
                    is Map<*, *> -> {
                        for (element in arg.values) {
                            if (element is Disposable) {
                                element.destroy()
                            }
                        }
                    }
                    is Iterable<*> -> {
                        for (element in arg) {
                            if (element is Disposable) {
                                element.destroy()
                            }
                        }
                    }
                }
            }
        }
    }
}

/**
 * @suppress
 */
inline fun <T : Disposable?, R> T.use(block: (T) -> R) =
    try {
        block(this)
    } finally {
        try {
            // N.B. our implementation is on the nullable type `Disposable?`.
            this?.destroy()
        } catch (e: Throwable) {
            // swallow
        }
    }

/** 
 * Placeholder object used to signal that we're constructing an interface with a FFI handle.
 *
 * This is the first argument for interface constructors that input a raw handle. It exists is that
 * so we can avoid signature conflicts when an interface has a regular constructor than inputs a
 * Long.
 *
 * @suppress
 * */
object UniffiWithHandle

/** 
 * Used to instantiate an interface without an actual pointer, for fakes in tests, mostly.
 *
 * @suppress
 * */
object NoHandle// Magic number for the Rust proxy to call using the same mechanism as every other method,
// to free the callback once it's dropped by Rust.
internal const val IDX_CALLBACK_FREE = 0
// Callback return codes
internal const val UNIFFI_CALLBACK_SUCCESS = 0
internal const val UNIFFI_CALLBACK_ERROR = 1
internal const val UNIFFI_CALLBACK_UNEXPECTED_ERROR = 2

/**
 * @suppress
 */
public abstract class FfiConverterCallbackInterface<CallbackInterface: Any>: FfiConverter<CallbackInterface, Long> {
    internal val handleMap = UniffiHandleMap<CallbackInterface>()

    internal fun drop(handle: Long) {
        handleMap.remove(handle)
    }

    override fun lift(value: Long): CallbackInterface {
        return handleMap.get(value)
    }

    override fun read(buf: ByteBuffer) = lift(buf.getLong())

    override fun lower(value: CallbackInterface) = handleMap.insert(value)

    override fun allocationSize(value: CallbackInterface) = 8UL

    override fun write(value: CallbackInterface, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}
/**
 * The cleaner interface for Object finalization code to run.
 * This is the entry point to any implementation that we're using.
 *
 * The cleaner registers objects and returns cleanables, so now we are
 * defining a `UniffiCleaner` with a `UniffiClenaer.Cleanable` to abstract the
 * different implmentations available at compile time.
 *
 * @suppress
 */
interface UniffiCleaner {
    interface Cleanable {
        fun clean()
    }

    fun register(value: Any, cleanUpTask: Runnable): UniffiCleaner.Cleanable

    companion object
}

// The fallback Jna cleaner, which is available for both Android, and the JVM.
private class UniffiJnaCleaner : UniffiCleaner {
    private val cleaner = com.sun.jna.internal.Cleaner.getCleaner()

    override fun register(value: Any, cleanUpTask: Runnable): UniffiCleaner.Cleanable =
        UniffiJnaCleanable(cleaner.register(value, cleanUpTask))
}

private class UniffiJnaCleanable(
    private val cleanable: com.sun.jna.internal.Cleaner.Cleanable,
) : UniffiCleaner.Cleanable {
    override fun clean() = cleanable.clean()
}


// We decide at uniffi binding generation time whether we were
// using Android or not.
// There are further runtime checks to chose the correct implementation
// of the cleaner.
private fun UniffiCleaner.Companion.create(): UniffiCleaner =
    try {
        // For safety's sake: if the library hasn't been run in android_cleaner = true
        // mode, but is being run on Android, then we still need to think about
        // Android API versions.
        // So we check if java.lang.ref.Cleaner is there, and use that…
        java.lang.Class.forName("java.lang.ref.Cleaner")
        JavaLangRefCleaner()
    } catch (e: ClassNotFoundException) {
        // … otherwise, fallback to the JNA cleaner.
        UniffiJnaCleaner()
    }

private class JavaLangRefCleaner : UniffiCleaner {
    val cleaner = java.lang.ref.Cleaner.create()

    override fun register(value: Any, cleanUpTask: Runnable): UniffiCleaner.Cleanable =
        JavaLangRefCleanable(cleaner.register(value, cleanUpTask))
}

private class JavaLangRefCleanable(
    val cleanable: java.lang.ref.Cleaner.Cleanable
) : UniffiCleaner.Cleanable {
    override fun clean() = cleanable.clean()
}

/**
 * @suppress
 */
public object FfiConverterUInt: FfiConverter<UInt, Int> {
    override fun lift(value: Int): UInt {
        return value.toUInt()
    }

    override fun read(buf: ByteBuffer): UInt {
        return lift(buf.getInt())
    }

    override fun lower(value: UInt): Int {
        return value.toInt()
    }

    override fun allocationSize(value: UInt) = 4UL

    override fun write(value: UInt, buf: ByteBuffer) {
        buf.putInt(value.toInt())
    }
}

/**
 * @suppress
 */
public object FfiConverterInt: FfiConverter<Int, Int> {
    override fun lift(value: Int): Int {
        return value
    }

    override fun read(buf: ByteBuffer): Int {
        return buf.getInt()
    }

    override fun lower(value: Int): Int {
        return value
    }

    override fun allocationSize(value: Int) = 4UL

    override fun write(value: Int, buf: ByteBuffer) {
        buf.putInt(value)
    }
}

/**
 * @suppress
 */
public object FfiConverterFloat: FfiConverter<Float, Float> {
    override fun lift(value: Float): Float {
        return value
    }

    override fun read(buf: ByteBuffer): Float {
        return buf.getFloat()
    }

    override fun lower(value: Float): Float {
        return value
    }

    override fun allocationSize(value: Float) = 4UL

    override fun write(value: Float, buf: ByteBuffer) {
        buf.putFloat(value)
    }
}

/**
 * @suppress
 */
public object FfiConverterBoolean: FfiConverter<Boolean, Byte> {
    override fun lift(value: Byte): Boolean {
        return value.toInt() != 0
    }

    override fun read(buf: ByteBuffer): Boolean {
        return lift(buf.get())
    }

    override fun lower(value: Boolean): Byte {
        return if (value) 1.toByte() else 0.toByte()
    }

    override fun allocationSize(value: Boolean) = 1UL

    override fun write(value: Boolean, buf: ByteBuffer) {
        buf.put(lower(value))
    }
}

/**
 * @suppress
 */
public object FfiConverterString: FfiConverter<String, RustBuffer.ByValue> {
    // Note: we don't inherit from FfiConverterRustBuffer, because we use a
    // special encoding when lowering/lifting.  We can use `RustBuffer.len` to
    // store our length and avoid writing it out to the buffer.
    override fun lift(value: RustBuffer.ByValue): String {
        try {
            val byteArr = ByteArray(value.len.toInt())
            value.asByteBuffer()!!.get(byteArr)
            return byteArr.toString(Charsets.UTF_8)
        } finally {
            RustBuffer.free(value)
        }
    }

    override fun read(buf: ByteBuffer): String {
        val len = buf.getInt()
        val byteArr = ByteArray(len)
        buf.get(byteArr)
        return byteArr.toString(Charsets.UTF_8)
    }

    fun toUtf8(value: String): ByteBuffer {
        // Make sure we don't have invalid UTF-16, check for lone surrogates.
        return Charsets.UTF_8.newEncoder().run {
            onMalformedInput(CodingErrorAction.REPORT)
            encode(CharBuffer.wrap(value))
        }
    }

    override fun lower(value: String): RustBuffer.ByValue {
        val byteBuf = toUtf8(value)
        // Ideally we'd pass these bytes to `ffi_bytebuffer_from_bytes`, but doing so would require us
        // to copy them into a JNA `Memory`. So we might as well directly copy them into a `RustBuffer`.
        val rbuf = RustBuffer.alloc(byteBuf.limit().toULong())
        rbuf.asByteBuffer()!!.put(byteBuf)
        return rbuf
    }

    // We aren't sure exactly how many bytes our string will be once it's UTF-8
    // encoded.  Allocate 3 bytes per UTF-16 code unit which will always be
    // enough.
    override fun allocationSize(value: String): ULong {
        val sizeForLength = 4UL
        val sizeForString = value.length.toULong() * 3UL
        return sizeForLength + sizeForString
    }

    override fun write(value: String, buf: ByteBuffer) {
        val byteBuf = toUtf8(value)
        buf.putInt(byteBuf.limit())
        buf.put(byteBuf)
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


//
public interface CrossEncoderInterface {
    
    /**
     * Rank documents by relevance to a query. Returns similarity scores.
     */
    suspend fun `rank`(`query`: kotlin.String, `documents`: List<kotlin.String>): List<kotlin.Float>
    
    /**
     * Rank documents and return them sorted by descending relevance.
     * Returns a JSON string of [document, score] pairs since UniFFI
     * doesn't support tuples directly.
     */
    suspend fun `rankAndSortJson`(`query`: kotlin.String, `documents`: List<kotlin.String>): kotlin.String
    
    companion object
}

open class CrossEncoder: Disposable, AutoCloseable, CrossEncoderInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }
    /**
     * Create a new cross-encoder for ranking documents by relevance.
     */
    constructor(`model`: Model, `contextSize`: kotlin.UInt?) :
        this(UniffiWithHandle, 
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_constructor_crossencoder_new(
    
        FfiConverterTypeModel.lower(`model`),FfiConverterOptionalUInt.lower(`contextSize`),_status)
}
    )

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_nobodywho_uniffi_fn_free_crossencoder(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_clone_crossencoder(handle, status)
        }
    }

    
    /**
     * Rank documents by relevance to a query. Returns similarity scores.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `rank`(`query`: kotlin.String, `documents`: List<kotlin.String>) : List<kotlin.Float> {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_crossencoder_rank(
                uniffiHandle,
                FfiConverterString.lower(`query`),FfiConverterSequenceString.lower(`documents`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterSequenceFloat.lift(it) },
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Rank documents and return them sorted by descending relevance.
     * Returns a JSON string of [document, score] pairs since UniFFI
     * doesn't support tuples directly.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `rankAndSortJson`(`query`: kotlin.String, `documents`: List<kotlin.String>) : kotlin.String {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_crossencoder_rank_and_sort_json(
                uniffiHandle,
                FfiConverterString.lower(`query`),FfiConverterSequenceString.lower(`documents`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterString.lift(it) },
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeCrossEncoder: FfiConverter<CrossEncoder, Long> {
    override fun lower(value: CrossEncoder): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): CrossEncoder {
        return CrossEncoder(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): CrossEncoder {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: CrossEncoder) = 8UL

    override fun write(value: CrossEncoder, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


//
public interface EncoderInterface {
    
    /**
     * Encode text into an embedding vector.
     */
    suspend fun `encode`(`text`: kotlin.String): List<kotlin.Float>
    
    companion object
}

open class Encoder: Disposable, AutoCloseable, EncoderInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }
    /**
     * Create a new encoder for generating text embeddings.
     */
    constructor(`model`: Model, `contextSize`: kotlin.UInt?) :
        this(UniffiWithHandle, 
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_constructor_encoder_new(
    
        FfiConverterTypeModel.lower(`model`),FfiConverterOptionalUInt.lower(`contextSize`),_status)
}
    )

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_nobodywho_uniffi_fn_free_encoder(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_clone_encoder(handle, status)
        }
    }

    
    /**
     * Encode text into an embedding vector.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `encode`(`text`: kotlin.String) : List<kotlin.Float> {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_encoder_encode(
                uniffiHandle,
                FfiConverterString.lower(`text`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterSequenceFloat.lift(it) },
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeEncoder: FfiConverter<Encoder, Long> {
    override fun lower(value: Encoder): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): Encoder {
        return Encoder(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): Encoder {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: Encoder) = 8UL

    override fun write(value: Encoder, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


//
public interface ModelInterface {
    
    companion object
}

open class Model: Disposable, AutoCloseable, ModelInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_nobodywho_uniffi_fn_free_model(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_clone_model(handle, status)
        }
    }

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeModel: FfiConverter<Model, Long> {
    override fun lower(value: Model): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): Model {
        return Model(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): Model {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: Model) = 8UL

    override fun write(value: Model, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


//
public interface RustChatInterface {
    
    /**
     * Send a message and get a token stream for the response.
     */
    fun `ask`(`message`: kotlin.String): RustTokenStream
    
    /**
     * Send a multimodal prompt (text + images/audio) and get a token stream.
     *
     * `parts` is an ordered list of `PromptPart` items.
     * Image and audio parts should contain a local file-system path.
     */
    fun `askWithPrompt`(`parts`: List<PromptPart>): RustTokenStream
    
    /**
     * Get the current chat history as a list of messages.
     */
    suspend fun `getChatHistory`(): List<Message>
    
    /**
     * Get the current sampler configuration as a JSON string.
     */
    suspend fun `getSamplerConfigJson`(): kotlin.String
    
    /**
     * Get the current system prompt.
     */
    suspend fun `getSystemPrompt`(): kotlin.String?
    
    /**
     * Get all template variables.
     */
    suspend fun `getTemplateVariables`(): Map<kotlin.String, kotlin.Boolean>
    
    /**
     * Reset the chat context with a new system prompt and tools.
     */
    suspend fun `resetContext`(`systemPrompt`: kotlin.String?, `tools`: List<RustTool>?)
    
    /**
     * Reset the chat history, keeping the system prompt and tools.
     */
    suspend fun `resetHistory`()
    
    /**
     * Set the chat history from a list of messages.
     */
    suspend fun `setChatHistory`(`messages`: List<Message>)
    
    /**
     * Set the sampler configuration.
     */
    suspend fun `setSamplerConfig`(`sampler`: SamplerConfig)
    
    /**
     * Set the system prompt.
     */
    suspend fun `setSystemPrompt`(`systemPrompt`: kotlin.String?)
    
    /**
     * Set a template variable.
     */
    suspend fun `setTemplateVariable`(`name`: kotlin.String, `value`: kotlin.Boolean)
    
    /**
     * Set the tools available to the model.
     */
    suspend fun `setTools`(`tools`: List<RustTool>)
    
    /**
     * Stop the current generation.
     */
    fun `stopGeneration`()
    
    companion object
}

open class RustChat: Disposable, AutoCloseable, RustChatInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }
    /**
     * Create a new chat session.
     */
    constructor(`model`: Model, `systemPrompt`: kotlin.String?, `contextSize`: kotlin.UInt, `templateVariables`: Map<kotlin.String, kotlin.Boolean>?, `tools`: List<RustTool>?, `sampler`: SamplerConfig?) :
        this(UniffiWithHandle, 
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_constructor_rustchat_new(
    
        FfiConverterTypeModel.lower(`model`),FfiConverterOptionalString.lower(`systemPrompt`),FfiConverterUInt.lower(`contextSize`),FfiConverterOptionalMapStringBoolean.lower(`templateVariables`),FfiConverterOptionalSequenceTypeRustTool.lower(`tools`),FfiConverterOptionalTypeSamplerConfig.lower(`sampler`),_status)
}
    )

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_nobodywho_uniffi_fn_free_rustchat(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_clone_rustchat(handle, status)
        }
    }

    
    /**
     * Send a message and get a token stream for the response.
     */override fun `ask`(`message`: kotlin.String): RustTokenStream {
            return FfiConverterTypeRustTokenStream.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_ask(
        it,
        FfiConverterString.lower(`message`),_status)
}
    }
    )
    }
    

    
    /**
     * Send a multimodal prompt (text + images/audio) and get a token stream.
     *
     * `parts` is an ordered list of `PromptPart` items.
     * Image and audio parts should contain a local file-system path.
     */override fun `askWithPrompt`(`parts`: List<PromptPart>): RustTokenStream {
            return FfiConverterTypeRustTokenStream.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_ask_with_prompt(
        it,
        FfiConverterSequenceTypePromptPart.lower(`parts`),_status)
}
    }
    )
    }
    

    
    /**
     * Get the current chat history as a list of messages.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `getChatHistory`() : List<Message> {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_get_chat_history(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterSequenceTypeMessage.lift(it) },
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Get the current sampler configuration as a JSON string.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `getSamplerConfigJson`() : kotlin.String {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_get_sampler_config_json(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterString.lift(it) },
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Get the current system prompt.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `getSystemPrompt`() : kotlin.String? {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_get_system_prompt(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterOptionalString.lift(it) },
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Get all template variables.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `getTemplateVariables`() : Map<kotlin.String, kotlin.Boolean> {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_get_template_variables(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterMapStringBoolean.lift(it) },
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Reset the chat context with a new system prompt and tools.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `resetContext`(`systemPrompt`: kotlin.String?, `tools`: List<RustTool>?) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_reset_context(
                uniffiHandle,
                FfiConverterOptionalString.lower(`systemPrompt`),FfiConverterOptionalSequenceTypeRustTool.lower(`tools`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Reset the chat history, keeping the system prompt and tools.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `resetHistory`() {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_reset_history(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Set the chat history from a list of messages.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setChatHistory`(`messages`: List<Message>) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_set_chat_history(
                uniffiHandle,
                FfiConverterSequenceTypeMessage.lower(`messages`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Set the sampler configuration.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setSamplerConfig`(`sampler`: SamplerConfig) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_set_sampler_config(
                uniffiHandle,
                FfiConverterTypeSamplerConfig.lower(`sampler`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Set the system prompt.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setSystemPrompt`(`systemPrompt`: kotlin.String?) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_set_system_prompt(
                uniffiHandle,
                FfiConverterOptionalString.lower(`systemPrompt`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Set a template variable.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setTemplateVariable`(`name`: kotlin.String, `value`: kotlin.Boolean) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_set_template_variable(
                uniffiHandle,
                FfiConverterString.lower(`name`),FfiConverterBoolean.lower(`value`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Set the tools available to the model.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setTools`(`tools`: List<RustTool>) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_set_tools(
                uniffiHandle,
                FfiConverterSequenceTypeRustTool.lower(`tools`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Stop the current generation.
     */override fun `stopGeneration`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_rustchat_stop_generation(
        it,
        _status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeRustChat: FfiConverter<RustChat, Long> {
    override fun lower(value: RustChat): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): RustChat {
        return RustChat(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): RustChat {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: RustChat) = 8UL

    override fun write(value: RustChat, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


//
public interface RustTokenStreamInterface {
    
    /**
     * Wait for the full response to complete and return it.
     */
    suspend fun `completed`(): kotlin.String
    
    /**
     * Get the next token. Returns None when generation is complete.
     */
    suspend fun `nextToken`(): kotlin.String?
    
    companion object
}

open class RustTokenStream: Disposable, AutoCloseable, RustTokenStreamInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_nobodywho_uniffi_fn_free_rusttokenstream(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_clone_rusttokenstream(handle, status)
        }
    }

    
    /**
     * Wait for the full response to complete and return it.
     */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `completed`() : kotlin.String {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rusttokenstream_completed(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterString.lift(it) },
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

    
    /**
     * Get the next token. Returns None when generation is complete.
     */
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `nextToken`() : kotlin.String? {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_method_rusttokenstream_next_token(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterOptionalString.lift(it) },
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeRustTokenStream: FfiConverter<RustTokenStream, Long> {
    override fun lower(value: RustTokenStream): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): RustTokenStream {
        return RustTokenStream(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): RustTokenStream {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: RustTokenStream) = 8UL

    override fun write(value: RustTokenStream, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


//
public interface RustToolInterface {
    
    /**
     * Get the JSON schema for this tool's parameters as a string.
     */
    fun `getSchemaJson`(): kotlin.String
    
    companion object
}

open class RustTool: Disposable, AutoCloseable, RustToolInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }
    /**
     * Create a tool that the model can call during inference.
     *
     * `parameters` is an ordered list of parameter definitions.
     * Each entry has a `name` and a `type` (e.g. `"string"`, `"integer"`, `"number"`, `"boolean"`).
     * The order matters — binding layers use it to map positional arguments
     * in the user's callback function to named JSON parameters.
     *
     * Supported types: `"string"`, `"integer"` / `"int"`, `"number"` / `"float"` / `"double"`,
     * `"boolean"` / `"bool"`.
     * A JSON schema is generated automatically from this list.
     *
     * The callback receives the model's arguments as a JSON string
     * (e.g. `{"city": "London", "degrees": 22}`).
     * Each binding layer wraps the user's function to parse this JSON and
     * pass individual typed arguments to the original function.
     */
    constructor(`name`: kotlin.String, `description`: kotlin.String, `parameters`: List<ToolParameter>, `callback`: RustToolCallback) :
        this(UniffiWithHandle, 
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_constructor_rusttool_new(
    
        FfiConverterString.lower(`name`),FfiConverterString.lower(`description`),FfiConverterSequenceTypeToolParameter.lower(`parameters`),FfiConverterTypeRustToolCallback.lower(`callback`),_status)
}
    )

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_nobodywho_uniffi_fn_free_rusttool(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_clone_rusttool(handle, status)
        }
    }

    
    /**
     * Get the JSON schema for this tool's parameters as a string.
     */override fun `getSchemaJson`(): kotlin.String {
            return FfiConverterString.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_rusttool_get_schema_json(
        it,
        _status)
}
    }
    )
    }
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeRustTool: FfiConverter<RustTool, Long> {
    override fun lower(value: RustTool): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): RustTool {
        return RustTool(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): RustTool {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: RustTool) = 8UL

    override fun write(value: RustTool, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


//
public interface SamplerBuilderInterface {
    
    /**
     * Sample from the probability distribution (weighted random selection).
     */
    fun `dist`(): SamplerConfig
    
    /**
     * DRY (Don't Repeat Yourself) sampler to reduce repetition.
     */
    fun `dry`(`multiplier`: kotlin.Float, `base`: kotlin.Float, `allowedLength`: kotlin.Int, `penaltyLastN`: kotlin.Int, `seqBreakers`: List<kotlin.String>): SamplerBuilder
    
    /**
     * Apply a grammar constraint to enforce structured output.
     */
    fun `grammar`(`grammar`: kotlin.String, `triggerOn`: kotlin.String?, `root`: kotlin.String): SamplerBuilder
    
    /**
     * Always select the most probable token (deterministic).
     */
    fun `greedy`(): SamplerConfig
    
    /**
     * Keep tokens with probability above min_p * (probability of most likely token).
     */
    fun `minP`(`minP`: kotlin.Float, `minKeep`: kotlin.UInt): SamplerBuilder
    
    /**
     * Use Mirostat v1 algorithm for perplexity-controlled sampling.
     */
    fun `mirostatV1`(`tau`: kotlin.Float, `eta`: kotlin.Float, `m`: kotlin.Int): SamplerConfig
    
    /**
     * Use Mirostat v2 algorithm for perplexity-controlled sampling.
     */
    fun `mirostatV2`(`tau`: kotlin.Float, `eta`: kotlin.Float): SamplerConfig
    
    /**
     * Apply repetition penalties to discourage repeated tokens.
     */
    fun `penalties`(`penaltyLastN`: kotlin.Int, `penaltyRepeat`: kotlin.Float, `penaltyFreq`: kotlin.Float, `penaltyPresent`: kotlin.Float): SamplerBuilder
    
    /**
     * Apply temperature scaling to the probability distribution.
     */
    fun `temperature`(`temperature`: kotlin.Float): SamplerBuilder
    
    /**
     * Keep only the top K most probable tokens.
     */
    fun `topK`(`topK`: kotlin.Int): SamplerBuilder
    
    /**
     * Keep tokens whose cumulative probability is below top_p.
     */
    fun `topP`(`topP`: kotlin.Float, `minKeep`: kotlin.UInt): SamplerBuilder
    
    /**
     * Typical sampling: keeps tokens close to expected information content.
     */
    fun `typicalP`(`typP`: kotlin.Float, `minKeep`: kotlin.UInt): SamplerBuilder
    
    /**
     * XTC sampler that probabilistically excludes high-probability tokens.
     */
    fun `xtc`(`xtcProbability`: kotlin.Float, `xtcThreshold`: kotlin.Float, `minKeep`: kotlin.UInt): SamplerBuilder
    
    companion object
}

open class SamplerBuilder: Disposable, AutoCloseable, SamplerBuilderInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }
    /**
     * Create a new SamplerBuilder to construct a custom sampler chain.
     */
    constructor() :
        this(UniffiWithHandle, 
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_constructor_samplerbuilder_new(
    
        _status)
}
    )

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_nobodywho_uniffi_fn_free_samplerbuilder(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_clone_samplerbuilder(handle, status)
        }
    }

    
    /**
     * Sample from the probability distribution (weighted random selection).
     */override fun `dist`(): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_dist(
        it,
        _status)
}
    }
    )
    }
    

    
    /**
     * DRY (Don't Repeat Yourself) sampler to reduce repetition.
     */override fun `dry`(`multiplier`: kotlin.Float, `base`: kotlin.Float, `allowedLength`: kotlin.Int, `penaltyLastN`: kotlin.Int, `seqBreakers`: List<kotlin.String>): SamplerBuilder {
            return FfiConverterTypeSamplerBuilder.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_dry(
        it,
        FfiConverterFloat.lower(`multiplier`),FfiConverterFloat.lower(`base`),FfiConverterInt.lower(`allowedLength`),FfiConverterInt.lower(`penaltyLastN`),FfiConverterSequenceString.lower(`seqBreakers`),_status)
}
    }
    )
    }
    

    
    /**
     * Apply a grammar constraint to enforce structured output.
     */override fun `grammar`(`grammar`: kotlin.String, `triggerOn`: kotlin.String?, `root`: kotlin.String): SamplerBuilder {
            return FfiConverterTypeSamplerBuilder.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_grammar(
        it,
        FfiConverterString.lower(`grammar`),FfiConverterOptionalString.lower(`triggerOn`),FfiConverterString.lower(`root`),_status)
}
    }
    )
    }
    

    
    /**
     * Always select the most probable token (deterministic).
     */override fun `greedy`(): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_greedy(
        it,
        _status)
}
    }
    )
    }
    

    
    /**
     * Keep tokens with probability above min_p * (probability of most likely token).
     */override fun `minP`(`minP`: kotlin.Float, `minKeep`: kotlin.UInt): SamplerBuilder {
            return FfiConverterTypeSamplerBuilder.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_min_p(
        it,
        FfiConverterFloat.lower(`minP`),FfiConverterUInt.lower(`minKeep`),_status)
}
    }
    )
    }
    

    
    /**
     * Use Mirostat v1 algorithm for perplexity-controlled sampling.
     */override fun `mirostatV1`(`tau`: kotlin.Float, `eta`: kotlin.Float, `m`: kotlin.Int): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_mirostat_v1(
        it,
        FfiConverterFloat.lower(`tau`),FfiConverterFloat.lower(`eta`),FfiConverterInt.lower(`m`),_status)
}
    }
    )
    }
    

    
    /**
     * Use Mirostat v2 algorithm for perplexity-controlled sampling.
     */override fun `mirostatV2`(`tau`: kotlin.Float, `eta`: kotlin.Float): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_mirostat_v2(
        it,
        FfiConverterFloat.lower(`tau`),FfiConverterFloat.lower(`eta`),_status)
}
    }
    )
    }
    

    
    /**
     * Apply repetition penalties to discourage repeated tokens.
     */override fun `penalties`(`penaltyLastN`: kotlin.Int, `penaltyRepeat`: kotlin.Float, `penaltyFreq`: kotlin.Float, `penaltyPresent`: kotlin.Float): SamplerBuilder {
            return FfiConverterTypeSamplerBuilder.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_penalties(
        it,
        FfiConverterInt.lower(`penaltyLastN`),FfiConverterFloat.lower(`penaltyRepeat`),FfiConverterFloat.lower(`penaltyFreq`),FfiConverterFloat.lower(`penaltyPresent`),_status)
}
    }
    )
    }
    

    
    /**
     * Apply temperature scaling to the probability distribution.
     */override fun `temperature`(`temperature`: kotlin.Float): SamplerBuilder {
            return FfiConverterTypeSamplerBuilder.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_temperature(
        it,
        FfiConverterFloat.lower(`temperature`),_status)
}
    }
    )
    }
    

    
    /**
     * Keep only the top K most probable tokens.
     */override fun `topK`(`topK`: kotlin.Int): SamplerBuilder {
            return FfiConverterTypeSamplerBuilder.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_top_k(
        it,
        FfiConverterInt.lower(`topK`),_status)
}
    }
    )
    }
    

    
    /**
     * Keep tokens whose cumulative probability is below top_p.
     */override fun `topP`(`topP`: kotlin.Float, `minKeep`: kotlin.UInt): SamplerBuilder {
            return FfiConverterTypeSamplerBuilder.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_top_p(
        it,
        FfiConverterFloat.lower(`topP`),FfiConverterUInt.lower(`minKeep`),_status)
}
    }
    )
    }
    

    
    /**
     * Typical sampling: keeps tokens close to expected information content.
     */override fun `typicalP`(`typP`: kotlin.Float, `minKeep`: kotlin.UInt): SamplerBuilder {
            return FfiConverterTypeSamplerBuilder.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_typical_p(
        it,
        FfiConverterFloat.lower(`typP`),FfiConverterUInt.lower(`minKeep`),_status)
}
    }
    )
    }
    

    
    /**
     * XTC sampler that probabilistically excludes high-probability tokens.
     */override fun `xtc`(`xtcProbability`: kotlin.Float, `xtcThreshold`: kotlin.Float, `minKeep`: kotlin.UInt): SamplerBuilder {
            return FfiConverterTypeSamplerBuilder.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerbuilder_xtc(
        it,
        FfiConverterFloat.lower(`xtcProbability`),FfiConverterFloat.lower(`xtcThreshold`),FfiConverterUInt.lower(`minKeep`),_status)
}
    }
    )
    }
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeSamplerBuilder: FfiConverter<SamplerBuilder, Long> {
    override fun lower(value: SamplerBuilder): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): SamplerBuilder {
        return SamplerBuilder(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): SamplerBuilder {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: SamplerBuilder) = 8UL

    override fun write(value: SamplerBuilder, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


//
public interface SamplerConfigInterface {
    
    /**
     * Serialize the sampler configuration to a JSON string.
     */
    fun `toJson`(): kotlin.String
    
    companion object
}

open class SamplerConfig: Disposable, AutoCloseable, SamplerConfigInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_nobodywho_uniffi_fn_free_samplerconfig(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_nobodywho_uniffi_fn_clone_samplerconfig(handle, status)
        }
    }

    
    /**
     * Serialize the sampler configuration to a JSON string.
     */
    @Throws(NobodyWhoException::class)override fun `toJson`(): kotlin.String {
            return FfiConverterString.lift(
    callWithHandle {
    uniffiRustCallWithError(NobodyWhoException) { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_method_samplerconfig_to_json(
        it,
        _status)
}
    }
    )
    }
    

    

    


    
    companion object {
        
    /**
     * Deserialize a sampler configuration from a JSON string.
     */
    @Throws(NobodyWhoException::class) fun `fromJson`(`jsonStr`: kotlin.String): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    uniffiRustCallWithError(NobodyWhoException) { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_constructor_samplerconfig_from_json(
    
        FfiConverterString.lower(`jsonStr`),_status)
}
    )
    }
    

        
    }
    
}


/**
 * @suppress
 */
public object FfiConverterTypeSamplerConfig: FfiConverter<SamplerConfig, Long> {
    override fun lower(value: SamplerConfig): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): SamplerConfig {
        return SamplerConfig(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): SamplerConfig {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: SamplerConfig) = 8UL

    override fun write(value: SamplerConfig, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}



data class Asset (
    var `id`: kotlin.String
    , 
    var `path`: kotlin.String
    
){
    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeAsset: FfiConverterRustBuffer<Asset> {
    override fun read(buf: ByteBuffer): Asset {
        return Asset(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: Asset) = (
            FfiConverterString.allocationSize(value.`id`) +
            FfiConverterString.allocationSize(value.`path`)
    )

    override fun write(value: Asset, buf: ByteBuffer) {
            FfiConverterString.write(value.`id`, buf)
            FfiConverterString.write(value.`path`, buf)
    }
}



data class ToolCall (
    var `name`: kotlin.String
    , 
    var `argumentsJson`: kotlin.String
    
){
    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeToolCall: FfiConverterRustBuffer<ToolCall> {
    override fun read(buf: ByteBuffer): ToolCall {
        return ToolCall(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: ToolCall) = (
            FfiConverterString.allocationSize(value.`name`) +
            FfiConverterString.allocationSize(value.`argumentsJson`)
    )

    override fun write(value: ToolCall, buf: ByteBuffer) {
            FfiConverterString.write(value.`name`, buf)
            FfiConverterString.write(value.`argumentsJson`, buf)
    }
}



data class ToolParameter (
    var `name`: kotlin.String
    , 
    var `type`: kotlin.String
    
){
    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeToolParameter: FfiConverterRustBuffer<ToolParameter> {
    override fun read(buf: ByteBuffer): ToolParameter {
        return ToolParameter(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: ToolParameter) = (
            FfiConverterString.allocationSize(value.`name`) +
            FfiConverterString.allocationSize(value.`type`)
    )

    override fun write(value: ToolParameter, buf: ByteBuffer) {
            FfiConverterString.write(value.`name`, buf)
            FfiConverterString.write(value.`type`, buf)
    }
}



sealed class Message {
    
    data class Message(
        val `role`: Role, 
        val `content`: kotlin.String, 
        val `assets`: List<Asset>) : Message()
        
    {
        

        companion object
    }
    
    data class ToolCalls(
        val `role`: Role, 
        val `content`: kotlin.String, 
        val `toolCalls`: List<ToolCall>) : Message()
        
    {
        

        companion object
    }
    
    data class ToolResp(
        val `role`: Role, 
        val `name`: kotlin.String, 
        val `content`: kotlin.String) : Message()
        
    {
        

        companion object
    }
    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeMessage : FfiConverterRustBuffer<Message>{
    override fun read(buf: ByteBuffer): Message {
        return when(buf.getInt()) {
            1 -> Message.Message(
                FfiConverterTypeRole.read(buf),
                FfiConverterString.read(buf),
                FfiConverterSequenceTypeAsset.read(buf),
                )
            2 -> Message.ToolCalls(
                FfiConverterTypeRole.read(buf),
                FfiConverterString.read(buf),
                FfiConverterSequenceTypeToolCall.read(buf),
                )
            3 -> Message.ToolResp(
                FfiConverterTypeRole.read(buf),
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: Message) = when(value) {
        is Message.Message -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeRole.allocationSize(value.`role`)
                + FfiConverterString.allocationSize(value.`content`)
                + FfiConverterSequenceTypeAsset.allocationSize(value.`assets`)
            )
        }
        is Message.ToolCalls -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeRole.allocationSize(value.`role`)
                + FfiConverterString.allocationSize(value.`content`)
                + FfiConverterSequenceTypeToolCall.allocationSize(value.`toolCalls`)
            )
        }
        is Message.ToolResp -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeRole.allocationSize(value.`role`)
                + FfiConverterString.allocationSize(value.`name`)
                + FfiConverterString.allocationSize(value.`content`)
            )
        }
    }

    override fun write(value: Message, buf: ByteBuffer) {
        when(value) {
            is Message.Message -> {
                buf.putInt(1)
                FfiConverterTypeRole.write(value.`role`, buf)
                FfiConverterString.write(value.`content`, buf)
                FfiConverterSequenceTypeAsset.write(value.`assets`, buf)
                Unit
            }
            is Message.ToolCalls -> {
                buf.putInt(2)
                FfiConverterTypeRole.write(value.`role`, buf)
                FfiConverterString.write(value.`content`, buf)
                FfiConverterSequenceTypeToolCall.write(value.`toolCalls`, buf)
                Unit
            }
            is Message.ToolResp -> {
                buf.putInt(3)
                FfiConverterTypeRole.write(value.`role`, buf)
                FfiConverterString.write(value.`name`, buf)
                FfiConverterString.write(value.`content`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}







sealed class NobodyWhoException: kotlin.Exception() {
    
    class Exception(
        
        val `message`: kotlin.String
        ) : NobodyWhoException() {
        override val message
            get() = "message=${ `message` }"
    }
    

    companion object ErrorHandler : UniffiRustCallStatusErrorHandler<NobodyWhoException> {
        override fun lift(error_buf: RustBuffer.ByValue): NobodyWhoException = FfiConverterTypeNobodyWhoError.lift(error_buf)
    }

    
}

/**
 * @suppress
 */
public object FfiConverterTypeNobodyWhoError : FfiConverterRustBuffer<NobodyWhoException> {
    override fun read(buf: ByteBuffer): NobodyWhoException {
        

        return when(buf.getInt()) {
            1 -> NobodyWhoException.Exception(
                FfiConverterString.read(buf),
                )
            else -> throw RuntimeException("invalid error enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: NobodyWhoException): ULong {
        return when(value) {
            is NobodyWhoException.Exception -> (
                // Add the size for the Int that specifies the variant plus the size needed for all fields
                4UL
                + FfiConverterString.allocationSize(value.`message`)
            )
        }
    }

    override fun write(value: NobodyWhoException, buf: ByteBuffer) {
        when(value) {
            is NobodyWhoException.Exception -> {
                buf.putInt(1)
                FfiConverterString.write(value.`message`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }

}



/**
 * A part of a multimodal prompt.  Mirrors the core `PromptPart` enum.
 */
sealed class PromptPart {
    
    data class Text(
        val `content`: kotlin.String) : PromptPart()
        
    {
        

        companion object
    }
    
    data class Image(
        val `path`: kotlin.String) : PromptPart()
        
    {
        

        companion object
    }
    
    data class Audio(
        val `path`: kotlin.String) : PromptPart()
        
    {
        

        companion object
    }
    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypePromptPart : FfiConverterRustBuffer<PromptPart>{
    override fun read(buf: ByteBuffer): PromptPart {
        return when(buf.getInt()) {
            1 -> PromptPart.Text(
                FfiConverterString.read(buf),
                )
            2 -> PromptPart.Image(
                FfiConverterString.read(buf),
                )
            3 -> PromptPart.Audio(
                FfiConverterString.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: PromptPart) = when(value) {
        is PromptPart.Text -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`content`)
            )
        }
        is PromptPart.Image -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`path`)
            )
        }
        is PromptPart.Audio -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`path`)
            )
        }
    }

    override fun write(value: PromptPart, buf: ByteBuffer) {
        when(value) {
            is PromptPart.Text -> {
                buf.putInt(1)
                FfiConverterString.write(value.`content`, buf)
                Unit
            }
            is PromptPart.Image -> {
                buf.putInt(2)
                FfiConverterString.write(value.`path`, buf)
                Unit
            }
            is PromptPart.Audio -> {
                buf.putInt(3)
                FfiConverterString.write(value.`path`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}






enum class Role {
    
    USER,
    ASSISTANT,
    SYSTEM,
    TOOL;
    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeRole: FfiConverterRustBuffer<Role> {
    override fun read(buf: ByteBuffer) = try {
        Role.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: Role) = 4UL

    override fun write(value: Role, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}







/**
 * Callback interface for tool functions.
 * Implement this in your language to provide the tool's logic.
 * The `call` method receives the tool arguments as a JSON string
 * and should return the tool's result as a string.
 */
public interface RustToolCallback {
    
    fun `call`(`argumentsJson`: kotlin.String): kotlin.String
    
    companion object
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceRustToolCallback {
    internal object `call`: UniffiCallbackInterfaceRustToolCallbackMethod0 {
        override fun callback(`uniffiHandle`: Long,`argumentsJson`: RustBuffer.ByValue,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeRustToolCallback.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`call`(
                    FfiConverterString.lift(`argumentsJson`),
                )
            }
            val writeReturn = { value: kotlin.String -> uniffiOutReturn.setValue(FfiConverterString.lower(value)) }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeRustToolCallback.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeRustToolCallback.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceRustToolCallback.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `call`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_nobodywho_uniffi_fn_init_callback_vtable_rusttoolcallback(vtable)
    }
}

/**
 * The ffiConverter which transforms the Callbacks in to handles to pass to Rust.
 *
 * @suppress
 */
public object FfiConverterTypeRustToolCallback: FfiConverterCallbackInterface<RustToolCallback>()




/**
 * @suppress
 */
public object FfiConverterOptionalUInt: FfiConverterRustBuffer<kotlin.UInt?> {
    override fun read(buf: ByteBuffer): kotlin.UInt? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterUInt.read(buf)
    }

    override fun allocationSize(value: kotlin.UInt?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterUInt.allocationSize(value)
        }
    }

    override fun write(value: kotlin.UInt?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterUInt.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalString: FfiConverterRustBuffer<kotlin.String?> {
    override fun read(buf: ByteBuffer): kotlin.String? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterString.read(buf)
    }

    override fun allocationSize(value: kotlin.String?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterString.allocationSize(value)
        }
    }

    override fun write(value: kotlin.String?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterString.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeSamplerConfig: FfiConverterRustBuffer<SamplerConfig?> {
    override fun read(buf: ByteBuffer): SamplerConfig? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeSamplerConfig.read(buf)
    }

    override fun allocationSize(value: SamplerConfig?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeSamplerConfig.allocationSize(value)
        }
    }

    override fun write(value: SamplerConfig?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeSamplerConfig.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalSequenceTypeRustTool: FfiConverterRustBuffer<List<RustTool>?> {
    override fun read(buf: ByteBuffer): List<RustTool>? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterSequenceTypeRustTool.read(buf)
    }

    override fun allocationSize(value: List<RustTool>?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterSequenceTypeRustTool.allocationSize(value)
        }
    }

    override fun write(value: List<RustTool>?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterSequenceTypeRustTool.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalMapStringBoolean: FfiConverterRustBuffer<Map<kotlin.String, kotlin.Boolean>?> {
    override fun read(buf: ByteBuffer): Map<kotlin.String, kotlin.Boolean>? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterMapStringBoolean.read(buf)
    }

    override fun allocationSize(value: Map<kotlin.String, kotlin.Boolean>?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterMapStringBoolean.allocationSize(value)
        }
    }

    override fun write(value: Map<kotlin.String, kotlin.Boolean>?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterMapStringBoolean.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceFloat: FfiConverterRustBuffer<List<kotlin.Float>> {
    override fun read(buf: ByteBuffer): List<kotlin.Float> {
        val len = buf.getInt()
        return List<kotlin.Float>(len) {
            FfiConverterFloat.read(buf)
        }
    }

    override fun allocationSize(value: List<kotlin.Float>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterFloat.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<kotlin.Float>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterFloat.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceString: FfiConverterRustBuffer<List<kotlin.String>> {
    override fun read(buf: ByteBuffer): List<kotlin.String> {
        val len = buf.getInt()
        return List<kotlin.String>(len) {
            FfiConverterString.read(buf)
        }
    }

    override fun allocationSize(value: List<kotlin.String>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterString.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<kotlin.String>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterString.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeRustTool: FfiConverterRustBuffer<List<RustTool>> {
    override fun read(buf: ByteBuffer): List<RustTool> {
        val len = buf.getInt()
        return List<RustTool>(len) {
            FfiConverterTypeRustTool.read(buf)
        }
    }

    override fun allocationSize(value: List<RustTool>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeRustTool.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<RustTool>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeRustTool.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeAsset: FfiConverterRustBuffer<List<Asset>> {
    override fun read(buf: ByteBuffer): List<Asset> {
        val len = buf.getInt()
        return List<Asset>(len) {
            FfiConverterTypeAsset.read(buf)
        }
    }

    override fun allocationSize(value: List<Asset>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeAsset.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<Asset>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeAsset.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeToolCall: FfiConverterRustBuffer<List<ToolCall>> {
    override fun read(buf: ByteBuffer): List<ToolCall> {
        val len = buf.getInt()
        return List<ToolCall>(len) {
            FfiConverterTypeToolCall.read(buf)
        }
    }

    override fun allocationSize(value: List<ToolCall>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeToolCall.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<ToolCall>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeToolCall.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeToolParameter: FfiConverterRustBuffer<List<ToolParameter>> {
    override fun read(buf: ByteBuffer): List<ToolParameter> {
        val len = buf.getInt()
        return List<ToolParameter>(len) {
            FfiConverterTypeToolParameter.read(buf)
        }
    }

    override fun allocationSize(value: List<ToolParameter>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeToolParameter.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<ToolParameter>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeToolParameter.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeMessage: FfiConverterRustBuffer<List<Message>> {
    override fun read(buf: ByteBuffer): List<Message> {
        val len = buf.getInt()
        return List<Message>(len) {
            FfiConverterTypeMessage.read(buf)
        }
    }

    override fun allocationSize(value: List<Message>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeMessage.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<Message>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeMessage.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypePromptPart: FfiConverterRustBuffer<List<PromptPart>> {
    override fun read(buf: ByteBuffer): List<PromptPart> {
        val len = buf.getInt()
        return List<PromptPart>(len) {
            FfiConverterTypePromptPart.read(buf)
        }
    }

    override fun allocationSize(value: List<PromptPart>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypePromptPart.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<PromptPart>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypePromptPart.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterMapStringBoolean: FfiConverterRustBuffer<Map<kotlin.String, kotlin.Boolean>> {
    override fun read(buf: ByteBuffer): Map<kotlin.String, kotlin.Boolean> {
        val len = buf.getInt()
        return buildMap<kotlin.String, kotlin.Boolean>(len) {
            repeat(len) {
                val k = FfiConverterString.read(buf)
                val v = FfiConverterBoolean.read(buf)
                this[k] = v
            }
        }
    }

    override fun allocationSize(value: Map<kotlin.String, kotlin.Boolean>): ULong {
        val spaceForMapSize = 4UL
        val spaceForChildren = value.map { (k, v) ->
            FfiConverterString.allocationSize(k) +
            FfiConverterBoolean.allocationSize(v)
        }.sum()
        return spaceForMapSize + spaceForChildren
    }

    override fun write(value: Map<kotlin.String, kotlin.Boolean>, buf: ByteBuffer) {
        buf.putInt(value.size)
        // The parens on `(k, v)` here ensure we're calling the right method,
        // which is important for compatibility with older android devices.
        // Ref https://blog.danlew.net/2017/03/16/kotlin-puzzler-whose-line-is-it-anyways/
        value.forEach { (k, v) ->
            FfiConverterString.write(k, buf)
            FfiConverterBoolean.write(v, buf)
        }
    }
}








        /**
         * Compute the cosine similarity between two vectors.
         */ fun `cosineSimilarity`(`a`: List<kotlin.Float>, `b`: List<kotlin.Float>): kotlin.Float {
            return FfiConverterFloat.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_func_cosine_similarity(
    
        FfiConverterSequenceFloat.lower(`a`),FfiConverterSequenceFloat.lower(`b`),_status)
}
    )
    }
    

        /**
         * Load a GGUF model from disk.
         * This is a free function instead of an async constructor because
         * uniffi-bindgen-react-native generates invalid JS (`async static` instead
         * of `static async`) for async constructors.
         */
    @Throws(NobodyWhoException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
     suspend fun `loadModel`(`modelPath`: kotlin.String, `useGpu`: kotlin.Boolean, `imageModelPath`: kotlin.String?) : Model {
        return uniffiRustCallAsync(
        UniffiLib.uniffi_nobodywho_uniffi_fn_func_load_model(FfiConverterString.lower(`modelPath`),FfiConverterBoolean.lower(`useGpu`),FfiConverterOptionalString.lower(`imageModelPath`),),
        { future, callback, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_poll_u64(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_nobodywho_uniffi_rust_future_complete_u64(future, continuation) },
        { future -> UniffiLib.ffi_nobodywho_uniffi_rust_future_free_u64(future) },
        // lift function
        { FfiConverterTypeModel.lift(it) },
        // Error FFI converter
        NobodyWhoException.ErrorHandler,
    )
    }

        /**
         * Get the default sampler configuration.
         */ fun `samplerPresetDefault`(): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_func_sampler_preset_default(
    
        _status)
}
    )
    }
    

        /**
         * Create a DRY sampler preset to reduce repetition.
         */ fun `samplerPresetDry`(): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_func_sampler_preset_dry(
    
        _status)
}
    )
    }
    

        /**
         * Create a sampler with a custom grammar constraint.
         */ fun `samplerPresetGrammar`(`grammar`: kotlin.String): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_func_sampler_preset_grammar(
    
        FfiConverterString.lower(`grammar`),_status)
}
    )
    }
    

        /**
         * Create a greedy sampler (always picks most probable token).
         */ fun `samplerPresetGreedy`(): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_func_sampler_preset_greedy(
    
        _status)
}
    )
    }
    

        /**
         * Create a sampler configured for JSON output generation.
         */ fun `samplerPresetJson`(): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_func_sampler_preset_json(
    
        _status)
}
    )
    }
    

        /**
         * Create a sampler with temperature scaling.
         */ fun `samplerPresetTemperature`(`temperature`: kotlin.Float): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_func_sampler_preset_temperature(
    
        FfiConverterFloat.lower(`temperature`),_status)
}
    )
    }
    

        /**
         * Create a sampler with top-k filtering only.
         */ fun `samplerPresetTopK`(`topK`: kotlin.Int): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_func_sampler_preset_top_k(
    
        FfiConverterInt.lower(`topK`),_status)
}
    )
    }
    

        /**
         * Create a sampler with nucleus (top-p) sampling.
         */ fun `samplerPresetTopP`(`topP`: kotlin.Float): SamplerConfig {
            return FfiConverterTypeSamplerConfig.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_nobodywho_uniffi_fn_func_sampler_preset_top_p(
    
        FfiConverterFloat.lower(`topP`),_status)
}
    )
    }
    


