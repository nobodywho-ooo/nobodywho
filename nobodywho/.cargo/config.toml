# Per-target rustflags for wasm builds:
# - +atomics,+bulk-memory,+mutable-globals: enable Emscripten pthreads,
#   so llama.cpp can multi-thread its matmuls inside the wasm instance.
# - panic=abort: wasm-bindgen requires `Closure` callbacks to be
#   UnwindSafe, but our `Chat` captures `Rc<RefCell<ChatState>>` in its
#   Worker callbacks (not RefUnwindSafe). Abort drops that requirement;
#   a panic tears down the module, like an unrecoverable ggml exception.
[target.wasm32-unknown-emscripten]
rustflags = [
    "-C", "panic=abort",
    "-C", "target-feature=+atomics,+bulk-memory,+mutable-globals",
]

# wasm64 (MEMORY64) uses the SAME rustflags — the 64-bit pointer width comes
# from the custom target spec (js/targets/wasm64-unknown-emscripten.json), not
# a target-feature. Keyed on that spec's file stem. The build script
# (build-pkg-emscripten-wasm64.sh) sets RUSTFLAGS explicitly to survive a
# `-D warnings` env override, same as the wasm32 script; this block is the
# fallback for a bare `cargo build --target …/wasm64-unknown-emscripten.json`.
[target.wasm64-unknown-emscripten]
rustflags = [
    "-C", "panic=abort",
    "-C", "target-feature=+atomics,+bulk-memory,+mutable-globals",
]
