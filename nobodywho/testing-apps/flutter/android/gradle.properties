# Flutter's template asks for -Xmx8G plus 4G of metaspace, which the Gradle
# daemon cannot always get (it is then killed with no output at all). This app
# builds fine well below that.
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
