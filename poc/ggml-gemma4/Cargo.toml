[package]
name = "ggml-gemma4-poc"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
ggml-runtime = { path = "../ggml-runtime" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
